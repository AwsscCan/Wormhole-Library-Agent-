/**
 * 队友02+03 模块目录 — lib/matching/
 *
 * 待实现文件：
 *   collision.ts     — 碰撞匹配评分（队友03 算法 + 队友02 数据）
 *   livingLibrary.ts — Living Book 检索与排序（队友02）
 *   consent.ts       — consent 状态机（队友02；private/paused 绝不返回）
 *
 * 隐私硬规则（不可妥协）：
 *   1. 推荐卡永远匿名。
 *   2. 双方同意前不暴露联系方式。
 *   3. matchingMode === "off" 时返回空。
 */
export type { LivingLibraryService } from "@/lib/types";
