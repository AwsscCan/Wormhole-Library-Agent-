import { NextResponse } from "next/server";
import { generateEvidenceDraft, WritingDependencyUnavailableError, WritingError } from "@/lib/writing/draftService";
import { resumeWriting } from "@/lib/writing/repository";
import { apiError, parseBody } from "@/lib/validation/api";
import { createDraftSchema } from "@/lib/validation/schemas";
import { privateWritingResponse, rejectWritingUserId, requireWritingPrincipal } from "@/lib/writing/routeSupport";
export async function GET(request: Request) { const rejected = rejectWritingUserId(request); if (rejected) return rejected; const principal = await requireWritingPrincipal(request); if (principal instanceof Response) return principal; const sessionId = new URL(request.url).searchParams.get("sessionId"); if (!sessionId) return privateWritingResponse(apiError("BAD_REQUEST", "sessionId is required", 400), principal); try { return privateWritingResponse(NextResponse.json(await resumeWriting(principal.id, sessionId)), principal); } catch { return privateWritingResponse(apiError("INTERNAL_ERROR", "Unable to resume writing", 500), principal); } }
export async function POST(request: Request) { const rejected = rejectWritingUserId(request); if (rejected) return rejected; const principal = await requireWritingPrincipal(request); if (principal instanceof Response) return principal; const parsed = await parseBody(request, createDraftSchema); if (!parsed.ok) return privateWritingResponse(parsed.response, principal); try { return privateWritingResponse(NextResponse.json(await generateEvidenceDraft({ ...parsed.data, principal }), { status: 201 }), principal); } catch (error) { if (error instanceof WritingDependencyUnavailableError) return privateWritingResponse(apiError("DEPENDENCY_UNAVAILABLE", "Writing dependencies are not configured", 503), principal); return privateWritingResponse(error instanceof WritingError ? apiError(error.code, error.message, error.code === "NOT_FOUND" ? 404 : error.code === "FORBIDDEN" ? 403 : 400) : apiError("INTERNAL_ERROR", "Unable to generate draft", 500), principal); } }
