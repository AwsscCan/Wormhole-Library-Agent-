import { NextResponse } from "next/server";
import { confirmCandidate, persistCandidate } from "@/lib/writing/repository";
import { apiError, parseBody } from "@/lib/validation/api";
import { candidateSchema, confirmCandidateSchema } from "@/lib/validation/schemas";
import { privateWritingResponse, rejectWritingUserId, requireWritingPrincipal } from "@/lib/writing/routeSupport";
export async function POST(request: Request) { const q = rejectWritingUserId(request); if (q) return q; const p = await requireWritingPrincipal(request); if (p instanceof Response) return p; const body = await parseBody(request, candidateSchema); if (!body.ok) return privateWritingResponse(body.response, p); try { await persistCandidate(p.id, body.data.sessionId, { ...body.data, verificationStatus: "needs_review" }); return privateWritingResponse(NextResponse.json({ id: body.data.id, verificationStatus: "needs_review" }, { status: 201 }), p); } catch { return privateWritingResponse(apiError("INTERNAL_ERROR", "Unable to save candidate", 500), p); } }
export async function PATCH(request: Request) { const q = rejectWritingUserId(request); if (q) return q; const p = await requireWritingPrincipal(request); if (p instanceof Response) return p; const body = await parseBody(request, confirmCandidateSchema); if (!body.ok) return privateWritingResponse(body.response, p); try { const confirmed = await confirmCandidate(p.id, body.data.sessionId, body.data.evidenceId); if (!confirmed) return privateWritingResponse(apiError("BAD_REQUEST", "Candidate cannot be verified", 400), p); return privateWritingResponse(NextResponse.json({ id: confirmed.id, verificationStatus: "verified", userConfirmedAt: confirmed.userConfirmedAt?.toISOString() }), p); } catch { return privateWritingResponse(apiError("INTERNAL_ERROR", "Unable to confirm candidate", 500), p); } }
